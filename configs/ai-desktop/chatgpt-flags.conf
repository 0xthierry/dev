# Use native Wayland so ChatGPT follows per-monitor scaling (the 4K display is
# scale 2 while the secondary display is scale 1).
--ozone-platform=wayland

# Force Chromium's Secret Service backend on Hyprland, which it does not
# recognize as a desktop environment with a keyring by default.
--password-store=gnome-libsecret
